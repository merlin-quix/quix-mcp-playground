name: HTTP API Weather Source
language: python
variables:
  - name: output
    inputType: OutputTopic
    description: This is the output topic for weather data
    defaultValue: configuration
    required: true
dockerfile: dockerfile
runEntryPoint: main.py
defaultFile: main.py
libraryItemId: web-api-gateway
